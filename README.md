The main file:
dual_panda.py


* Right now the robots are just placed like that without paying much attention just to prove a point. Maybe we have to fix all of these (can be done also later).

  * We have to also find the right contact model and parameters so that the simulations is not slowed down too much.
